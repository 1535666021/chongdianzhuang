# DELIVERY.md — v30 返工单：片区分组口径修正

- 任务：片区分组口径修正（甲方原话："只要有单（哪怕只有1单），片区标签就全部显示。"）
- 版本：v30 ｜ commit：见 git log 最新一条
- 交付时间：2026-07-20
- 历史交付：v29 任务R（表单预设化+数据贯通+首页片区+话术入口）、v28 任务Q（话术模板库），详见 git 历史

## 改动文件清单（仅 1 个文件，禁越界已核验）

| 文件 | 改动 |
|---|---|
| `src/lib/geoCluster.ts` | ①取消片区 ≥2 单门槛：clusterOrdersByArea 不再过滤 1 单片区，全部片区生成 chip；②新增同名片区合并：坐标聚类/文本归组产生的同名片区（如两个"槐林镇"、两个"未命名片区"）按名合一，保证一个片区一个 chip、chip 单数=该片实际待办总数；③片区名 trim（修正" 亚父街道"前导空格类数据） |

其他一切文件零改动：parser.ts / statistics.ts 与 v29 逐字节一致（已 diff 核验）。

## 自测结果（node 直跑 lib，恢复出厂密码 147568 真实数据，15 PASS / 0 FAIL）

```
PASS | 恢复出厂 140 单
PASS | 片区chip总数 7 个（旧口径 1 单片区全隐藏）
PASS | 活跃单 9 全部出现在片区中，无遗漏
PASS | 含1单片区 5 个，全部显示
PASS | chip 单数逐片区核验一致：未命名片区(2)/槐林镇(2)/合肥巢湖苏湾镇(1)/瑶海区(1)/烔炀镇(1)/亚父街道(1)/凤凰山街道(1)
PASS | 1单片区可进入批量预约流程（可约单数正确）
PASS | 5月 55单/6512.40、6月 50单/8308.50、7月 20单/对账6570.40/实际5780.40 零回归
```

工程级：`tsc --noEmit` 0 错误；`vite build` 通过。
